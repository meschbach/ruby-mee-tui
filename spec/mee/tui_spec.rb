require "spec_helper"

RSpec.describe Mee::Tui do
  it "has a version number" do
    expect(Mee::Tui::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
